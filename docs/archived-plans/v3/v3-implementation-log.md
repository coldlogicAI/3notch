- 2026-05-25: Landed folder-canonical packet bundles, `artifacts[]`, `nextSteps`, manifest hash verification, scanner/size enforcement, MCP/CLI `--file` copy + `--ref`, and built-in `.notchpkt` pack/unpack; covered by focused unit/CLI/MCP/schema tests (`npm test -- tests/unit/artifact-service.test.ts tests/unit/archive-service.test.ts tests/unit/packet-service.test.ts tests/unit/transfer-service.test.ts tests/unit/store-service.test.ts tests/unit/secret-scan-service.test.ts tests/unit/audit-service.test.ts tests/cli/packet.test.ts tests/schema/packet-schema.test.ts tests/mcp/packet-tools.test.ts tests/unit/record-factory.test.ts`).
- 2026-05-25: Added V3 e2e smokes for brand handoff, pack/unpack, tamper rejection, size caps, scanner-on-artifact, V2 back-compat, and `--ref`; `npm run test:e2e` passed.
- 2026-05-25: Plan deviation — Step 3.3's per-client prompt edits landed in `src/cli/prompts/shared.ts` (the single renderer all five clients use) rather than touching `claude-code.ts` / `claude-desktop.ts` / `codex.ts` / `cursor.ts` / `claude-chat.ts` individually. Those per-client files are metadata-only stubs.
- 2026-05-25: Post-review tightening — exposed `assertImmutablePacketFolder` from `store-service.ts` and called it before bundle writes so the Step 1.3 fail-loud contract is real (not just implied by collision suffixing); added unit coverage for `packet.md` / `manifest.json` / `artifacts/*` overwrite attempts. Added a tarbomb-rejection unit test (path traversal, absolute, unexpected top-level entry). Added a V2 single-file pack/unpack round-trip test. Dropped a dead `mtime: 0` cast in `gzipSync` (option is not part of zlib's type and was silently ignored; archive determinism still holds via Node's default-zeroed gzip header timestamp).
- 2026-05-26: Release-readiness polish — private packet create/import/unpack/mark/reply/seed outputs now tell users to use `--private`, invalid `--file path:purpose` labels remain structured errors, and copied artifacts preserve project-relative paths under `artifacts/`; covered by targeted CLI/unit/MCP/schema tests (`npm test -- tests/unit/artifact-service.test.ts tests/cli/packet.test.ts tests/unit/archive-service.test.ts tests/unit/transfer-service.test.ts tests/mcp/packet-tools.test.ts tests/schema/packet-schema.test.ts`).
